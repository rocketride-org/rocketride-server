name: Docker Publish

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Server version to package (e.g. 1.0.3)'
        required: true

permissions:
  contents: read
  packages: write

jobs:
  publish:
    name: Build & push engine image
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'workflow_dispatch' ||
      startsWith(github.event.release.tag_name, 'server-v')
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Determine version
        id: version
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_VERSION: ${{ inputs.version }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            echo "version=${INPUT_VERSION}" >> $GITHUB_OUTPUT
            echo "tag=server-v${INPUT_VERSION}" >> $GITHUB_OUTPUT
          else
            echo "version=${RELEASE_TAG#server-v}" >> $GITHUB_OUTPUT
            echo "tag=${RELEASE_TAG}" >> $GITHUB_OUTPUT
          fi

      - name: Download server artifacts
        run: |
          gh release download "${{ steps.version.outputs.tag }}" \
            --pattern "rocketride-server-*-linux-x64.tar.gz" \
            --dir /tmp/release

          gh release download "${{ steps.version.outputs.tag }}" \
            --pattern "rocketride-server-*-linux-arm64.tar.gz" \
            --dir /tmp/release
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Extract server binaries
        run: |
          mkdir -p dist/server/amd64 dist/server/arm64
          tar xf /tmp/release/rocketride-server-*-linux-x64.tar.gz -C dist/server/amd64
          tar xf /tmp/release/rocketride-server-*-linux-arm64.tar.gz -C dist/server/arm64
          if [ ! -f dist/server/amd64/engine ]; then
            echo "::error::amd64 engine binary not found"
            ls -la dist/server/amd64/
            exit 1
          fi
          if [ ! -f dist/server/arm64/engine ]; then
            echo "::error::arm64 engine binary not found"
            ls -la dist/server/arm64/
            exit 1
          fi

      - name: Set up QEMU
        uses: docker/setup-qemu-action@29109295f81e9208d7d86ff1c6c12d2833863392 # v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Log in to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: .
          file: docker/Dockerfile.engine
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ghcr.io/${{ github.repository_owner }}/rocketride-engine:${{ steps.version.outputs.version }}
            ghcr.io/${{ github.repository_owner }}/rocketride-engine:latest
