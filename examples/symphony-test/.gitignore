# The telemetry database id, remembered between runs so sessions accumulate.
# Local state, not source: a checkout should create its own store on first run.
.telemetry-db.json
# Rendered MIDI files are run artifacts, regenerated by every run.
output/
__pycache__/
