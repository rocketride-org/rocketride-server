[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rocketride-mcp"
version = "1.3.0"
description = "[DEPRECATED] RocketRide MCP stdio server — use the engine's built-in HTTP MCP server instead"
classifiers = [
  "Development Status :: 7 - Inactive",
]
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "RocketRide Development Team", email = "dev@rocketride.ai" }
]
license = "MIT"
dependencies = [
  "mcp>=2.0.0,<3",
  "rocketride>=1.0.4",
]

[project.urls]
Homepage = "https://rocketride.ai"
Repository = "https://github.com/rocketride/rocketride-clients"

[project.scripts]
rocketride-mcp = "rocketride_mcp.server:main"
rocketride-mcp-sse = "rocketride_mcp.sse_server:main"

[project.optional-dependencies]
sse = [
  "uvicorn>=0.30.0",
  "starlette>=0.38.0",
]
dev = [
  "ruff>=0.8.0",
  "pytest>=7.0.0",
  "pytest-asyncio>=0.21.0",
  "python-dotenv>=1.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.setuptools.packages.find]
where = ["src"]
include = ["rocketride_mcp*"]

[tool.setuptools.package-data]
rocketride_mcp = ["pipelines/*.json"]
