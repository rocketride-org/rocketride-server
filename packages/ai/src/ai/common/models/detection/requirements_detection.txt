# Object detection / segmentation backends.
#
# Permissive-license engines (RF-DETR, MM-Grounding-DINO, Mask2Former) load
# via the Hugging Face transformers stack. transformers itself
# is pinned per-node (see e.g. nodes/src/nodes/detect/requirements.txt) so it
# installs at module-load time before the loader __init__ runs.
#
# RF-DETR has its own optional PyPI package; loaders fall back to the
# Apache-2.0 RT-DETR checkpoints via transformers when it is unavailable.

Pillow
timm
rfdetr; python_version >= "3.10"
