# Core dependencies for Store
aiofiles

# Retry logic for cloud storage providers (s3, azure)
tenacity

# Optional cloud storage backends — install only if needed:
#   boto3                          (S3 support)
#   azure-storage-blob             (Azure Blob Storage)
#   azure-identity                 (Azure credential chain)
