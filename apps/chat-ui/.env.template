# Dev-only. The app never bakes a server address — it targets the origin it
# was served from (the dev server proxies /task to the engine on 5565).
ROCKETRIDE_APIKEY={insert your RocketRide APIKEY here}
ROCKETRIDE_OPENAI_KEY={insert your OpenAI APIKEY here}
