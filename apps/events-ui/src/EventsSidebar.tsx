// =============================================================================
// EVENTS-UI — Sidebar (empty — controls are embedded in the app)
// =============================================================================

import React from 'react';
import type { ShellSidebarProps } from 'shell-ui';

const EventsSidebar: React.FC<ShellSidebarProps> = (_props) => {
	return null;
};

export default EventsSidebar;
