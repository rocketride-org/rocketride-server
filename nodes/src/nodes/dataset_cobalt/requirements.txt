# depends() resolves this file relative to this node's own directory and
# no-ops when it is missing, so the driver has to be declared here even though
# eval_cobalt declares the same one. The two are reconciled, not deduplicated:
# lock-node-deps.yml compiles every nodes/src/nodes/**/requirements.txt into
# nodes/src/nodes/constraints.lock and fails the PR if they resolve to
# incompatible versions, and ensure_constraints() applies that same solve with
# -c at install time. Keep the specifier in step with
# ../eval_cobalt/requirements.txt; nodes/test/cobalt pulls that one in with -r.
basalt-ai-cobalt>=0.1.0,<1.0.0
