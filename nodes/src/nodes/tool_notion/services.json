{
	//
	// Required:
	//		The displayable name of this node
	//
	"title": "Notion",
	//
	// Required:
	//		The protocol is the endpoint protocol
	//
	"protocol": "tool_notion://",
	//
	// Required:
	//		Class type of the node - what it does
	//
	"classType": ["tool"],
	//
	// Required:
	//		Capabilities are flags that change the behavior of the underlying engine.
	//		"experimental" marks this node as not yet production-ready — the request
	//		and response shapes here are read from Notion's own API reference, but no
	//		live workspace has exercised it end-to-end.
	//
	"capabilities": ["invoke", "experimental"],
	//
	// Optional:
	//		Register is either filter, endpoint or ignored if not specified.
	//
	"register": "filter",
	//
	// Optional:
	//		The node is the actual physical node to instantiate.
	//
	"node": "python",
	//
	// Optional:
	//		The path is the executable/script code.
	//
	"path": "nodes.tool_notion",
	//
	// Required:
	//		The prefix map when converting URLs <=> paths.
	//
	"prefix": "notion",
	//
	// Optional:
	//		The icon to display in the UI for this node.
	//
	"icon": "notion.svg",
	//
	// Optional:
	//		Description of this node shown in the pipeline builder.
	//
	"description": ["Exposes a Notion workspace (search, pages, databases) as agent tools.", "Search, read, and write Notion pages and databases using an internal integration secret — search by title, query database rows, read a page's body as plain text, and create/update/append pages."],
	"tile": [],
	"lanes": {},
	//
	// Optional:
	//		Profile section - configuration options used by the driver itself.
	//		The default profile is applied when the node is first added to a pipeline.
	//
	"preconfig": {
		"default": "default",
		"profiles": {
			"default": {
				"title": "Notion",
				"apikey": ""
			}
		}
	},
	//
	// Optional:
	//		Local field definitions. These define the config fields exposed in the UI.
	//		"secure: true" fields are encrypted at rest and masked in the UI.
	//
	"fields": {
		"tool_notion.apikey": {
			"type": "string",
			"title": "API Key",
			"description": "Notion internal integration secret (from https://www.notion.so/my-integrations)",
			"default": "",
			"secure": true,
			"ui": {
				"ui:widget": "ApiKeyWidget"
			}
		}
	},
	//
	//	Test configuration for automated node testing.
	//	"apikey" is intentionally set to a placeholder, config validation is
	//	tested here, not live API calls (no verified workspace to test against).
	//
	"test": {
		"profiles": ["default"],
		"outputs": [],
		"cases": [
			{
				"name": "Config validation with placeholder key",
				"text": "test query"
			}
		]
	},
	//
	// Required:
	//		Defines the fields (shape) shown in the pipeline builder side panel.
	//
	"shape": [
		{
			"section": "Pipe",
			"title": "Notion",
			"properties": ["type", "tool_notion.apikey"]
		}
	]
}
