# caption (Describe)

A RocketRide image-filter node that generates a natural-language description of an
image using a local vision-language model — **SmolVLM-500M-Instruct** (default,
small and fast), **Qwen3-VL 4B**, or **Mage-VL 4B** for richer descriptions on
GPU. All Apache-2.0. No API key required.

## What it does

Receives an image on its input lane, runs the selected vision-language model with
a steerable prompt, and emits the description on the text lane. SmolVLM (Idefics3)
and Qwen3-VL load through the native `transformers` auto-mapping; Mage-VL ships
its own modeling code and loads with `trust_remote_code=True`, pinned to an exact
commit sha so the executed code is immutable (see Profiles). The default prompt
asks for a detailed description; you can also focus it ("Describe the people in
this image") or ask a question about the image ("What color is the car?").

Inference is delegated to the `Captioner` facade
(`ai.common.models.vision.caption`), which runs on the model server when
`--modelserver` is set, else locally. It runs on CPU, Apple Silicon (MPS), and
CUDA (bfloat16 on CUDA, float32 elsewhere) with greedy decoding. Generation is
bounded by a configurable token cap (256 by default) and, optionally, a sentence
budget that stops decoding at a clean sentence boundary instead of a mid-sentence
token cut. Inputs are downscaled to a 1536 px long edge before
inference (quality-neutral; trims the model-server payload). Local inference is
guarded by a 60 s watchdog; if inference fails for a frame, the node logs a
warning and emits an empty string so the pipeline keeps flowing.

For object detection use **Object Detection**. For text reading use **OCR**.

---

## Configuration

### Lanes

| Lane | Direction | Description |
|------|-----------|-------------|
| `image` | input | AVI image stream to describe |
| `text` | output | Description string |

### Fields

| Field | Type | Description |
|---|---|---|
| `caption.prompt` | string | Default "Describe this image in detail.". Steers the description; can focus on aspects or ask a question about the image |
| `caption.max_new_tokens` | integer | Default 256. Hard cap on generated tokens per description; bounds worst-case latency |
| `caption.max_sentences` | integer | Default 0 (off). Stop decoding after this many complete sentences — a clean cut instead of a mid-sentence token cut |
| `caption.profile` | string | Default "smolvlm-500m". Selects the vision-language model |

---

## Profiles

| Profile | Model | Notes |
|---------|-------|-------|
| `smolvlm-500m` (default) | `HuggingFaceTB/SmolVLM-500M-Instruct` | Apache-2.0; pinned to revision `a7da5b986cb59b408707209984f360a5f4ad7e47` |
| `qwen3-vl-4b` | `Qwen/Qwen3-VL-4B-Instruct` | Apache-2.0; native `transformers` (>= 4.57); pinned to revision `ebb281ec70b05090aa6165b016eac8ec08e71b17`; ~9 GB bf16 weights, GPU recommended |
| `mage-vl-4b` | `microsoft/Mage-VL` | Apache-2.0; **loads with `trust_remote_code=True`** (the repo ships its own modeling code) — mitigated by pinning revision `5c78cab61938e73859b63724d9bf5cb88c477eaa`, which the loader enforces and passes to `from_pretrained` so the executed remote code is immutable; ~9.5 GB bf16 weights, GPU recommended |

---

<!-- ROCKETRIDE:GENERATED:PARAMS START -->
<!-- Generated by nodes:docs-generate. Do not edit by hand. -->

## Schema

| Field | Type | Description | Default |
|---|---|---|---|
| `caption.profile` | `string` | **Model** | `"smolvlm-500m"` |
| `caption.prompt` | `string` | **Prompt**<br/>Steers the description. The default asks for a detailed description; you can also focus it ("Describe the people in this image") or ask a question about the image ("What color is the car?"). | `"Describe this image in detail."` |

## Source

[<svg viewBox="0 0 16 16" width="15" height="15" fill="currentColor" aria-hidden="true" style="vertical-align:-0.15em;margin-right:0.35em"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg> View source](https://github.com/rocketride-org/rocketride-server/tree/develop/nodes/src/nodes/caption)
<!-- ROCKETRIDE:GENERATED:PARAMS END -->
