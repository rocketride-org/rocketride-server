---
title: Transformer Embedding
date: 2026-04-08
sidebar_position: 1
---

## What it does

Generates text embeddings using local sentence-transformer models. Runs on the model server — no API key required. GPU-accelerated when available.

**Lanes:**

| Lane in     | Lane out    | Description                                           |
| ----------- | ----------- | ----------------------------------------------------- |
| `documents` | `documents` | Embed document chunks, attach vector to each document |
| `questions` | `questions` | Embed a question for vector similarity lookup         |

The `questions` lane is used when querying a vector store — the store expects an embedded question to compare against stored document vectors.

## Configuration

| Field | Description                          |
| ----- | ------------------------------------ |
| Model | Embedding model (see profiles below) |

**Custom model options** (shown when Custom is selected):

| Field               | Description                                       |
| ------------------- | ------------------------------------------------- |
| Model name          | Any Hugging Face sentence-transformer model       |
| Truncate dimensions | Reduce embedding size (0 = model default)         |
| Document prefix     | Prefix prepended to document text before encoding |
| Query prefix        | Prefix prepended to query text before encoding    |

## Profiles

| Profile            | Model                                              | Notes                         |
| ------------------ | -------------------------------------------------- | ----------------------------- |
| miniLM _(default)_ | `sentence-transformers/multi-qa-MiniLM-L6-cos-v1`  | General use, good performance |
| miniAll            | `sentence-transformers/all-MiniLM-L6-v2`           | General use alternative       |
| mpnet              | `sentence-transformers/multi-qa-mpnet-base-cos-v1` | Higher quality                |
| Custom             | _(user-specified)_                                 | Any Hugging Face model        |

## Reference

<!-- ROCKETRIDE:GENERATED:PARAMS START -->
<!-- Generated by nodes:docs-generate. Do not edit by hand. -->

- **Class type** — embedding
- **Capabilities** — gpu
- **Protocol** — `embedding_transformer://`

**Data lanes**

- `documents` → `documents`
- `questions` → `questions`

**Profiles**

- `custom` — Custom model
- `miniLM` — miniLM  - General use embeddings, good performance · model `sentence-transformers/multi-qa-MiniLM-L6-cos-v1`
- `miniAll` — miniAll - Another good one · model `sentence-transformers/all-MiniLM-L6-v2`
- `mpnet` — mpnet   - Yet another good one · model `sentence-transformers/multi-qa-mpnet-base-cos-v1`

**Configuration sections**

- **Sentence Transformer** — `embedding.profile`

**Schema**

- **Model name** (`embedding.model`) — `string`. Hugging face model to use for embedding
- **Truncate dimensions** (`embedding.truncate_dim`) — `number`. Truncate embeddings to this dimensionality (0 = use model default)
- **Document prefix** (`embedding.document_prefix`) — `string`. Prefix prepended to document text before encoding (e.g. 'search_document: ', 'passage: ')
- **Query prefix** (`embedding.query_prefix`) — `string`. Prefix prepended to query text before encoding (e.g. 'search_query: ', 'query: ')
- **Model** (`embedding.profile`) — `string`, default `miniLM`. Embedding model
- `embedding.preprocessor` — default `preprocessor_langchain`

### Dependencies

- `numpy`

### Classes

**`IGlobal.py` — `IGlobal(IGlobalBase)`**

- `beginGlobal(self)`
- `endGlobal(self)`

**`IInstance.py` — `IInstance(IInstanceBase)`**

Represent an instance that processes documents and questions using embeddings.

- `open(self, obj: Entry)` — Initialize the instance with a given entry.
- `writeDocuments(self, documents: List[Doc])` — Encode and processes a list of documents.
- `writeQuestions(self, question: Question)` — Encode a single question.
- `close(self)`

### Source

[<svg viewBox="0 0 16 16" width="15" height="15" fill="currentColor" aria-hidden="true" style="vertical-align:-0.15em;margin-right:0.35em"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg> GitHub/embedding_transformer](https://github.com/rocketride-org/rocketride-server/tree/develop/nodes/src/nodes/embedding_transformer)
<!-- ROCKETRIDE:GENERATED:PARAMS END -->
