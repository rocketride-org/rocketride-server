---
title: Bland AI
date: 2026-04-08
sidebar_position: 1
---

## What it does

Gives agents the ability to make and manage AI-powered phone calls via the Bland AI API. Useful for automating outbound calls — scheduling, surveys, follow-ups, or any task that requires a voice interaction.

## Tools

| Tool                 | Description                                              |
| -------------------- | -------------------------------------------------------- |
| `bland.make_call`    | Initiate an outbound AI phone call                       |
| `bland.get_call`     | Retrieve call status, transcript, recording, and summary |
| `bland.analyze_call` | Run post-call AI analysis against a set of questions     |

### bland.make_call

| Parameter        | Required | Description                             |
| ---------------- | -------- | --------------------------------------- |
| `phone_number`   | yes      | E.164 format (e.g. `+15551234567`)      |
| `task`           | yes      | Instructions for the AI during the call |
| `first_sentence` | no       | Opening line spoken by the AI           |
| `voice`          | no       | Override the default voice              |
| `max_duration`   | no       | Override the default max call length    |
| `webhook`        | no       | URL to receive call events              |

Returns a `call_id` for use with `get_call` and `analyze_call`.

### bland.get_call

| Parameter             | Required | Description                                    |
| --------------------- | -------- | ---------------------------------------------- |
| `call_id`             | yes      | ID returned by `make_call`                     |
| `wait_for_completion` | no       | Poll until the call finishes (up to 5 minutes) |

### bland.analyze_call

| Parameter   | Required | Description                                            |
| ----------- | -------- | ------------------------------------------------------ |
| `call_id`   | yes      | ID returned by `make_call`                             |
| `goal`      | no       | Context about the call's purpose                       |
| `questions` | no       | Array of `[question, expected_type]` pairs to evaluate |

## Configuration

| Field          | Description                                                               |
| -------------- | ------------------------------------------------------------------------- |
| API Key        | Bland AI API key (`org_xxx`)                                              |
| Tool Namespace | Prefix for tool names (default: `bland`)                                  |
| Voice          | Default AI voice: June, Josh, Nat, Paige, Derek, Florian (default: June)  |
| Max Duration   | Default max call length in minutes (default: 5)                           |
| Record         | Record calls by default (default: on)                                     |
| Language       | Default call language: `en`, `es`, `fr`, `de`, `zh`, `ja` (default: `en`) |

## Upstream docs

- [Bland AI documentation](https://docs.bland.ai)

## Reference

<!-- ROCKETRIDE:GENERATED:PARAMS START -->
<!-- Generated by nodes:docs-generate. Do not edit by hand. -->

- **Class type** — tool
- **Capabilities** — invoke
- **Protocol** — `tool_bland_ai://`

**Profiles**

- `default` — Default

**Configuration sections**

- **Bland AI Voice Agent** — `type`, `bland_ai.apikey`, `bland_ai.voice`, `bland_ai.maxDuration`, `bland_ai.language`, `bland_ai.record`

**Schema**

- **Server name** (`bland_ai.serverName`) — `string`, default `bland`. Namespace prefix for the tools: &lt;serverName&gt;.make_call, &lt;serverName&gt;.get_call, &lt;serverName&gt;.analyze_call
- **API Key** (`bland_ai.apikey`) — `string`. Bland AI API key (org_xxx). Get one at https://www.bland.ai
- **Default Voice** (`bland_ai.voice`) — `string`, default `June`. Default AI voice for calls
- **Max Call Duration (minutes)** (`bland_ai.maxDuration`) — `integer`, default `5`. Maximum call length in minutes (positive integer)
- **Record Calls** (`bland_ai.record`) — `boolean`, default `true`
- **Language** (`bland_ai.language`) — `string`, default `en`

### Dependencies

- `requests`

### Classes

**`IGlobal.py` — `IGlobal(IGlobalBase)`**

Global state for tool_bland_ai.

- `beginGlobal(self) -> None`
- `validateConfig(self) -> None`
- `endGlobal(self) -> None`

**`IInstance.py` — `IInstance(IInstanceBase)`**

- `invoke(self, param: Any) -> Any`

### Source

[<svg viewBox="0 0 16 16" width="15" height="15" fill="currentColor" aria-hidden="true" style="vertical-align:-0.15em;margin-right:0.35em"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg> GitHub/tool_bland_ai](https://github.com/rocketride-org/rocketride-server/tree/develop/nodes/src/nodes/tool_bland_ai)
<!-- ROCKETRIDE:GENERATED:PARAMS END -->
