requests
numpy
huggingface_hub>=0.20.0
# Piper (all profiles share this file via depends): in-process ONNX synthesis
piper-tts>=1.2.0,<2
# Bark: Hugging Face ``text-to-audio`` pipeline (torch from engine / model-server image)
transformers
# Kokoro-82M: official inference package (not transformers AutoModel)
kokoro>=0.9.4
soundfile>=0.12.0
# en_core_web_sm is installed dynamically in tts_engine._kokoro() via
# ai.common.models.audio.spacy_en_model.ensure_spacy_en_model() so the version
# matches the installed spaCy
# WAV→MP3 in-process via lameenc (no ffmpeg subprocess)
lameenc>=1.6.0
