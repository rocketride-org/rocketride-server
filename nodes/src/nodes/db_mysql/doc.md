---
title: MySQL
date: 2026-04-08
sidebar_position: 1
---

<head>
  <title>MySQL - RocketRide Documentation</title>
</head>

## What it does

MySQL node with two roles: pipeline node (natural-language queries via lanes) and tool node (agents call it directly).

## Connections

| Connection | Required | Description                                    |
| ---------- | -------- | ---------------------------------------------- |
| `llm`      | yes      | LLM used to generate SQL from natural language |

## As a pipeline node

**Lanes:**

| Lane in     | Lane out  | Description                                           |
| ----------- | --------- | ----------------------------------------------------- |
| `questions` | `table`   | Translate question → SQL → execute, return as table   |
| `questions` | `text`    | Translate question → SQL → execute, return as text    |
| `questions` | `answers` | Translate question → SQL → execute, return as answers |
| `answers`   | —         | Parse structured data and insert into table           |

Auto-creates the target table on first insert if it doesn't exist.

## As a tool

When connected to an agent, exposes three functions under the configured server name (default: `mysql`):

| Function           | Description                                                              |
| ------------------ | ------------------------------------------------------------------------ |
| `mysql.get_data`   | Natural language → SQL → execute, returns rows (default 250, max 25 000) |
| `mysql.get_schema` | Returns tables, columns, types, primary keys, and foreign keys           |
| `mysql.get_sql`    | Natural language → SQL only — no execution                               |

Only `SELECT` is permitted for queries. Insert operations use the `answers` lane.

## Configuration

| Field                   | Default     | Description                                                              |
| ----------------------- | ----------- | ------------------------------------------------------------------------ |
| Database Description    | —           | Plain-language description of the database, used to guide SQL generation |
| Host                    | `localhost` | MySQL server address                                                     |
| User                    | `root`      | Database username                                                        |
| Password                | —           | Database password                                                        |
| Database                | —           | Database name                                                            |
| Table                   | —           | Target table name                                                        |
| Max Validation Attempts | `5`         | Retry limit for EXPLAIN-based SQL validation (range 1–20)                |

## SQL validation

Generated SQL is validated by running `EXPLAIN` against the live database. If validation fails, the error is fed back to the LLM for a corrected query. This repeats up to **Max Validation Attempts** times before the node raises an error.

## Reference

<!-- ROCKETRIDE:GENERATED:PARAMS START -->
<!-- Generated by nodes:docs-generate. Do not edit by hand. -->

- **Class type** — database, tool
- **Capabilities** — noremote, invoke
- **Protocol** — `db_mysql://`

**Data lanes**

- `answers` — ingest lane
- `questions` → `table`, `text`, `answers`

**Profiles**

- `default`

**Configuration sections**

- **MySQL** — `mysql.profile`

**Schema**

- **MySQL host** (`mysql.host`) — `string`, default `localhost`. Host name or IP address of the MySQL server
- **User** (`mysql.user`) — `string`, default `root`. User to connect to the MySQL server
- **Password** (`mysql.password`) — `string`. Password to connect to the MySQL server
- **Database name** (`mysql.database`) — `string`, default `database`. Name of database
- **Table name** (`mysql.table`) — `string`, default `table`. Name of table
- **Database description** (`mysql.db_description`) — `string`. What is this database used for? Describe its content and purpose — this helps the LLM generate more accurate queries.
- **Max validation attempts** (`mysql.max_attempts`) — `integer`, default `5`. Maximum number of times to re-ask the LLM if EXPLAIN rejects the generated SQL
- **Allow direct query execution** (`mysql.allow_execute`) — `boolean`, default `false`. Permit QuestionType.EXECUTE callers to run raw SQL without LLM translation or safety checks. Leave OFF unless a trusted application explicitly needs to issue SQL directly.
- `mysql.profile` — `string`, default `default`

### Dependencies

- `pymysql` `==1.2.0`
- `cryptography` `==46.0.7`

### Classes

**`IGlobal.py` — `IGlobal(DatabaseGlobalBase)`**

MySQL-specific global state.

**`IInstance.py` — `IInstance(DatabaseInstanceBase)`**

MySQL-specific instance.

### Source

[<svg viewBox="0 0 16 16" width="15" height="15" fill="currentColor" aria-hidden="true" style="vertical-align:-0.15em;margin-right:0.35em"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg> GitHub/db_mysql](https://github.com/rocketride-org/rocketride-server/tree/develop/nodes/src/nodes/db_mysql)
<!-- ROCKETRIDE:GENERATED:PARAMS END -->
