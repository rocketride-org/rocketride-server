---
title: Python
date: 2026-04-08
sidebar_position: 1
---

## What it does

Gives agents the ability to execute Python code in a sandboxed environment. Useful for data manipulation, calculations, formatting, and any logic the agent needs to run directly rather than describe.

## Tools

| Tool             | Description                                   |
| ---------------- | --------------------------------------------- |
| `python.execute` | Execute a Python script and return its output |

### python.execute

| Parameter | Required | Description                   |
| --------- | -------- | ----------------------------- |
| `code`    | yes      | Python source code to execute |

**Response:**

```json
{
	"stdout": "...",
	"stderr": "...",
	"exit_code": 0,
	"timed_out": false,
	"result": null
}
```

`exit_code` is `0` on success, `1` on exception, `-1` on timeout. If the script assigns a value to a variable named `result`, it is returned in the `result` field.

## Configuration

| Field           | Description                                            |
| --------------- | ------------------------------------------------------ |
| Tool Namespace  | Prefix for the tool name (default: `python`)           |
| Timeout         | Max execution time in seconds (default: 20, max: 1200) |
| Allowed Modules | Additional modules to allow beyond the defaults        |

## Sandbox

Code runs in a restricted in-process sandbox using RestrictedPython. Network access, filesystem access, and subprocess execution are not available. Only the following modules are importable by default:

`math`, `cmath`, `decimal`, `fractions`, `statistics`, `random`, `string`, `textwrap`, `re`, `json`, `csv`, `collections`, `itertools`, `functools`, `operator`, `copy`, `dataclasses`, `enum`, `typing`, `datetime`, `time`, `calendar`, `base64`, `hashlib`, `hmac`, `struct`, `difflib`, `pprint`, `bisect`, `heapq`, `array`, `numbers`, `unicodedata`

Additional modules can be whitelisted in configuration.

## Reference

<!-- ROCKETRIDE:GENERATED:PARAMS START -->
<!-- Generated by nodes:docs-generate. Do not edit by hand. -->

- **Class type** — tool
- **Capabilities** — invoke
- **Protocol** — `tool_python://`

**Profiles**

- `default` — Default

**Configuration sections**

- **Python** — `type`, `tool_python.timeout`, `tool_python.allowedModules`

**Schema**

- **Server name** (`tool_python.serverName`) — `string`, default `python`. Namespace prefix for the tool: &lt;serverName&gt;.execute
- **Module name** (`tool_python.moduleName`) — `string`
- **Execution timeout (seconds)** (`tool_python.timeout`) — `integer`, default `20`. Maximum seconds a script may run before it is killed. Useful for long-running operations like network scans. Default is 20s, max is 1200s.
- **Additional Allowed Modules** (`tool_python.allowedModules`) — `array`. Modules the agent is allowed to import, in addition to the built-in defaults (math, json, re, collections, datetime, etc.).

### Classes

**`IGlobal.py` — `IGlobal(IGlobalBase)`**

Global state for tool_python.

- `beginGlobal(self) -> None`
- `endGlobal(self) -> None`

**`IInstance.py` — `IInstance(IInstanceBase)`**

- `execute(self, args)` — Execute Python code in a sandboxed environment.

### Source

[<svg viewBox="0 0 16 16" width="15" height="15" fill="currentColor" aria-hidden="true" style="vertical-align:-0.15em;margin-right:0.35em"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg> GitHub/tool_python](https://github.com/rocketride-org/rocketride-server/tree/develop/nodes/src/nodes/tool_python)
<!-- ROCKETRIDE:GENERATED:PARAMS END -->
