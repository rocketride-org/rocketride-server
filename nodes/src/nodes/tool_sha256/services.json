{
	"title": "Tool — SHA-256",
	"protocol": "tool_sha256://",
	"classType": ["tool"],
	"capabilities": ["invoke"],
	"register": "filter",
	"node": "python",
	"path": "nodes.tool_sha256",
	"prefix": "sha256",
	"documentation": "https://docs.rocketride.org",
	"description": ["Reference multimodal tool. Computes the SHA-256 of a binary attachment supplied via a FileStore path.", "Used as a fixture for tool-attachment integration tests."],
	"tile": ["SHA-256"],
	"lanes": {},
	"preconfig": {
		"default": "default",
		"profiles": {
			"default": {
				"title": "Default"
			}
		}
	},
	"fields": {},
	"shape": [
		{
			"section": "Pipe",
			"title": "SHA-256",
			"properties": ["type"]
		}
	]
}
