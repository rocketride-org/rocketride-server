# xTrace Memory

Long-term, shared agent memory exposed as **tools**, backed by
[xTrace Memory Manager](https://docs.mem.xtrace.ai).

This is a `tool` node, not the agent's `memory` subsystem. RocketRide's `memory` classType is
a single, run-scoped scratchpad (e.g. `memory_internal`, `put`/`get`/`peek`) that the agent
uses internally and excludes from tool discovery. xTrace is different: an external service for
**long-term, semantic, shared** recall that the agent calls explicitly via tools. It therefore
*complements* the required short-term memory subsystem rather than replacing it — wire both.

Anything one agent remembers can be recalled by another agent pointing at the same org — and,
via groups, across users.

## What it does

You send conversation turns; xTrace runs server-side LLM extraction to pull out **facts**
(and optionally **artifacts**) and **episodes**, embeds them, and stores them in your org's
memory pool. Later you retrieve the relevant slice with natural-language search — optionally
LLM-composed into a ready-to-inject context block. That extraction + relevance selection is
the "reasoning" layer a plain vector store doesn't give you.

## Agent tools

| Tool | xTrace API | Purpose |
| --- | --- | --- |
| `xtrace.remember` | `POST /v1/memories` | Ingest conversation turns. Returns the ingest job (terminal when synchronous). |
| `xtrace.recall` | `POST /v1/memories/search` | Search the pool. In `compose` mode returns a ready-to-inject `context` block plus the ranked rows. |

## Wiring

This is a `tool` node — wire it to an agent via `control` (class `tool`), alongside the
agent's required `memory` node:

```jsonc
{
  "id": "tool_xtrace_memory_1",
  "provider": "tool_xtrace_memory",
  "config": { "type": "tool_xtrace_memory" },
  "control": [{ "classType": "tool", "from": "agent_rocketride_1" }]
}
```

The agent discovers `xtrace.remember` / `xtrace.recall` as tools and calls them per its
instructions. Point several agents (in one pipe or across runs) at the same `org_id`
(+ `group_ids`) to give them one shared brain.

## Configuration

| Field | Required | Notes |
| --- | --- | --- |
| `api_key` | yes | xTrace API key (`xtk_…`). Also reads `XTRACE_API_KEY`. Stored encrypted. |
| `org_id` | yes | xTrace org id (`org_…`). Also reads `XTRACE_ORG_ID`. |
| `base_url` | no | Defaults to `https://api.production.xtrace.ai` (use the staging URL for testing). |
| `user_id` | for ingest | Default scope key. Required on ingest unless the agent passes one per call. |
| `agent_id` / `app_id` | no | Optional scopes stamped on ingest and AND-narrowed on recall. |
| `group_ids` | no | Comma-separated `grp_…` ids for **shared** memory across users/agents. |
| `wait` | no | Synchronous ingest (waits ~30s for extraction). Default on. |
| `ingest_timeout` | no | Max seconds to wait/poll for a terminal ingest job. |
| `extract_artifacts` | no | Opt into the (expensive) artifact stage. Facts/episodes are always extracted. |
| `search_mode` | no | `compose` (ready-to-inject context) or `retrieve` (rows only). |
| `search_limit` | no | Max rows per recall. |

## Where to get your credentials

Get them from the **Developer Portal: [app.xtrace.ai](https://app.xtrace.ai) → Settings →
API Keys** — copy your **Org id** and create an **API key (`xtk_…`)**. They are not in
`mem.xtrace.ai`.

Every request sends `x-api-key` + `X-Org-Id`. Never commit keys — use node config (encrypted)
or env vars.

> Note: the xTrace Python SDK is on their roadmap; this node talks to the documented HTTP
> API directly.

## Reference

<!-- ROCKETRIDE:GENERATED:PARAMS START -->
<!-- Generated by nodes:docs-generate. Do not edit by hand. -->

- **Class type** — tool
- **Capabilities** — invoke
- **Protocol** — `tool_xtrace_memory://`

**Profiles**

- `default` — xTrace Memory

**Configuration sections**

- **xTrace Memory** — `type`, `xtrace_memory.api_key`, `xtrace_memory.org_id`, `xtrace_memory.user_id`, `xtrace_memory.group_ids`, `xtrace_memory.show_advanced`

**Schema**

- **API Key** (`xtrace_memory.api_key`) — `string`. xTrace API key (xtk_...). Get it from the &lt;b&gt;Developer Portal&lt;/b&gt;: &lt;a href='https://app.xtrace.ai' target='_blank'&gt;app.xtrace.ai&lt;/a&gt; → Settings → API Keys (not in mem.xtrace.ai).
- **Org id** (`xtrace_memory.org_id`) — `string`. xTrace organization id (org_...). Same place as the API key: &lt;a href='https://app.xtrace.ai' target='_blank'&gt;app.xtrace.ai&lt;/a&gt; → Settings → API Keys.
- **User id** (`xtrace_memory.user_id`) — `string`. Who this memory belongs to (used to store and recall). Can be passed by the agent per call.
- **Group ids (shared memory)** (`xtrace_memory.group_ids`) — `string`. Optional. Comma-separated group ids (grp_...) to share memory across users/agents. Leave empty for private memory.
- **Advanced settings** (`xtrace_memory.show_advanced`) — `boolean`, default `false`. Show advanced options. The defaults work for most cases — leave off for a simple setup.
- **Base URL** (`xtrace_memory.base_url`) — `string`, default `https://api.production.xtrace.ai`. xTrace Memory API base URL. Production by default; use https://api.staging.xtrace.ai for staging.
- **Agent id** (`xtrace_memory.agent_id`) — `string`. Optional agent scope stamped on ingested memories and AND-narrowed on recall.
- **App id** (`xtrace_memory.app_id`) — `string`. Optional app scope stamped on ingested memories and AND-narrowed on recall.
- **Synchronous ingest** (`xtrace_memory.wait`) — `boolean`, default `true`. When on, ingest waits (up to ~30s) for extraction to finish so the agent gets a terminal result. When off, ingest returns immediately and runs in the background.
- **Ingest timeout (s)** (`xtrace_memory.ingest_timeout`) — `integer`, default `30`. Max seconds to wait/poll for an ingest job to reach a terminal state.
- **Extract artifacts** (`xtrace_memory.extract_artifacts`) — `boolean`, default `false`. Opt into artifact extraction (the most expensive stage). Facts and episodes are always extracted. Leave off unless you need it.
- **Recall mode** (`xtrace_memory.search_mode`) — `string`, default `compose`. 'compose' returns a ready-to-inject markdown context block (LLM-selected); 'retrieve' returns ranked rows only.
- **Recall limit** (`xtrace_memory.search_limit`) — `integer`, default `10`. Maximum number of memory rows to retrieve per recall.

### Dependencies

- `requests`
- `tenacity`

### Classes

**`IGlobal.py` — `IGlobal(IGlobalBase)`**

Global state for xtrace_memory.

- `beginGlobal(self) -> None`
- `validateConfig(self) -> None`
- `endGlobal(self) -> None`

**`IInstance.py` — `IInstance(IInstanceBase)`**

Node instance exposing xTrace Memory as agent tools.

- `beginInstance(self) -> None`
- `open(self, _obj: Any) -> None` — Anchor a fresh conversation id per client session (no store wipe).
- `remember(self, args)` — Ingest conversation turns into xTrace memory.
- `recall(self, args)` — Search xTrace memory and return relevant context.

### Source

[<svg viewBox="0 0 16 16" width="15" height="15" fill="currentColor" aria-hidden="true" style="vertical-align:-0.15em;margin-right:0.35em"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg> GitHub/tool_xtrace_memory](https://github.com/rocketride-org/rocketride-server/tree/develop/nodes/src/nodes/tool_xtrace_memory)
<!-- ROCKETRIDE:GENERATED:PARAMS END -->
