{
	"title": "xTrace Memory",
	"protocol": "tool_xtrace_memory://",
	"classType": ["tool"],
	"capabilities": ["invoke"],
	"register": "filter",
	"node": "python",
	"path": "nodes.tool_xtrace_memory",
	"prefix": "xtrace",
	"icon": "xtrace.svg",
	"documentation": "https://docs.mem.xtrace.ai",
	"description": ["Long-term, shared agent memory exposed as tools, backed by xTrace Memory Manager.", "Exposes two agent tools: 'remember' stores conversation turns and 'recall' returns the relevant, ready-to-inject context. Set your API key and org id and it works; point several agents at the same org (and group) to share one memory."],
	"tile": ["Org: ${parameters.xtrace_memory.org_id}"],
	"lanes": {},
	"preconfig": {
		"default": "default",
		"profiles": {
			"default": {
				"title": "xTrace Memory",
				"api_key": "",
				"org_id": "",
				"user_id": "",
				"group_ids": ""
			}
		}
	},
	"fields": {
		"xtrace_memory.api_key": {
			"type": "string",
			"title": "API Key",
			"description": "xTrace API key (xtk_...). Create one at app.xtrace.ai → Settings → API Keys.",
			"default": "",
			"secure": true,
			"ui": {
				"ui:widget": "ApiKeyWidget"
			}
		},
		"xtrace_memory.org_id": {
			"type": "string",
			"title": "Org id",
			"description": "xTrace organization id (org_...).",
			"default": ""
		},
		"xtrace_memory.user_id": {
			"type": "string",
			"optional": true,
			"title": "User id",
			"description": "Who this memory belongs to (used to store and recall). Defaults can be passed by the agent per call.",
			"default": ""
		},
		"xtrace_memory.group_ids": {
			"type": "string",
			"optional": true,
			"title": "Group ids (shared memory)",
			"description": "Optional. Comma-separated group ids (grp_...) to share memory across users/agents. Leave empty for private memory.",
			"default": ""
		}
	},
	"test": {
		"profiles": ["default"],
		"outputs": [],
		"cases": [
			{
				"name": "Config validation with placeholder credentials",
				"text": "test"
			}
		]
	},
	"shape": [
		{
			"section": "Pipe",
			"title": "xTrace Memory",
			"properties": ["type", "xtrace_memory.api_key", "xtrace_memory.org_id", "xtrace_memory.user_id", "xtrace_memory.group_ids"]
		}
	]
}
