{
	"title": "Gmail",
	"protocol": "tool_gmail://",
	"classType": ["tool"],
	"capabilities": ["invoke"],
	"register": "filter",
	"node": "python",
	"path": "nodes.tool_google_workspace.gmail",
	"prefix": "gmail",
	"icon": "gmail.svg",
	"documentation": "https://docs.rocketride.org",
	"description": [
		"Exposes Gmail operations as agent tools.\n",
		"Read, search, label, draft, send, and organize mail; permanent delete available with full access.\n",
		"Authenticates via a Google service account or user OAuth."
	],
	"lanes": {},
	"propertyDefinitions": {
		"access": {
			"type": "string",
			"title": "Access level",
			"description": [
				"Gmail scopes to request. readonly: read only. modify: read + label/organize. send: modify + send mail. settings: ",
				"modify + filters/IMAP/POP/vacation/forwarding. settings_sharing: settings + sendAs/delegation/S⁠MIME. full: ",
				"complete mailbox access, required for permanent delete."
			],
			"default": "modify",
			"enum": [
				["readonly", "Read-only"],
				["modify", "Modify (read + organize)"],
				["send", "Send (modify + send mail)"],
				["settings", "Settings (modify + filters/IMAP/vacation/forwarding)"],
				["settings_sharing", "Settings sharing (settings + sendAs/delegation/S⁠MIME)"],
				["full", "Full (allows permanent delete)"]
			]
		},
		"allowHardDelete": {
			"type": "boolean",
			"title": "Allow permanent delete",
			"description": [
				"Enable permanent message/thread deletion (requires full access tier). Disabled by default to protect against ",
				"accidental data loss."
			],
			"default": false
		}
	},
	"properties": ["type", "google.authType", "access"]
}
