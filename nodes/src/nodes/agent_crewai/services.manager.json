{
	"title": "CrewAI",
	"protocol": "agent_crewai_manager://",
	"classType": ["agent"],
	"capabilities": ["invoke"],
	"register": "filter",
	"node": "python",
	"path": "nodes.agent_crewai",
	"prefix": "agent",
	"description": ["Runs as a standalone agent or orchestrates a team of CrewAI sub-agents using hierarchical delegation.", "Connect CrewAI Agent nodes to enable multi-agent mode; without them it runs as a single agent."],
	"icon": "crewai.svg",
	"invoke": {
		"llm": {
			"description": "LLM used by the agent",
			"min": 1
		},
		"tool": {
			"description": "Tools available to the agent",
			"min": 0
		},
		"crewai": {
			"description": "Connected CrewAI Agent sub-agent nodes. When connected, runs as a hierarchical crew.",
			"min": 0
		}
	},
	"lanes": {
		"questions": ["answers"]
	},
	"input": [
		{
			"lane": "questions",
			"output": [{ "lane": "answers" }]
		}
	],
	"preconfig": {
		"default": "default",
		"profiles": {
			"default": {
				"instructions": [],
				"advanced_mode": false,
				"role": "",
				"goal": "",
				"backstory": "",
				"task_description": "",
				"expected_output": ""
			}
		}
	},
	"fields": {
		"instructions": {
			"type": "array",
			"title": "Instructions",
			"description": "Additional instructions to guide the agent.",
			"items": {
				"type": "string",
				"format": "textarea"
			}
		},
		"advanced_mode": {
			"type": "boolean",
			"title": "Advanced Mode",
			"description": "Expose CrewAI Agent and Task configuration directly.",
			"default": false,
			"enum": [
				[false, "Off"],
				[true, "On"]
			],
			"conditional": [
				{
					"value": true,
					"properties": ["role", "goal", "backstory", "task_description", "expected_output"]
				}
			]
		},
		"role": {
			"type": "string",
			"title": "Role",
			"description": "Agent role name (e.g. 'Senior Analyst'). Maps to CrewAI Agent(role=...)."
		},
		"goal": {
			"type": "string",
			"format": "textarea",
			"title": "Goal",
			"description": "What this agent is trying to achieve. Maps to CrewAI Agent(goal=...)."
		},
		"backstory": {
			"type": "string",
			"format": "textarea",
			"title": "Backstory",
			"description": "Background context for this agent's persona. Maps to CrewAI Agent(backstory=...)."
		},
		"task_description": {
			"type": "string",
			"format": "textarea",
			"title": "Task",
			"description": "What this agent should do. If blank, the incoming question is used. Maps to CrewAI Task(description=...)."
		},
		"expected_output": {
			"type": "string",
			"format": "textarea",
			"title": "Expected Output",
			"description": "Description of the expected output format. Maps to CrewAI Task(expected_output=...)."
		},
		"agent_crewai_manager.default": {
			"object": "default",
			"properties": ["instructions", "advanced_mode"]
		}
	},
	"shape": [
		{
			"section": "Pipe",
			"title": "CrewAI",
			"properties": ["agent_crewai_manager.default"]
		}
	]
}
