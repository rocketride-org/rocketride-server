# Python Client API (`rocketride-client-python`)

The full Python client API reference lives in the package and is kept up to date with each release.

**→ [packages/client-python/README.md](../../packages/client-python/README.md)**

When you install `rocketride-client-python`, the same content is in the package README.
