# TypeScript Client API (`@rocketride/client-typescript`)

The full TypeScript client API reference lives in the package and is kept up to date with each release.

**→ [packages/client-typescript/README.md](../../packages/client-typescript/README.md)**

When you install `@rocketride/client-typescript`, the same content is in the package README (e.g. `node_modules/@rocketride/client-typescript/README.md`).
